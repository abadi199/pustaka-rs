{
  "name": "pustaka-rs",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "rustup": "rustup override set nightly-2018-05-15",
    "postinstall": "cargo install --version 1.9.2 watchexec",
    "watch": "watchexec --restart -w src 'elm make src/client/Main.elm --output app/app.js; npm run start:server'",
    "build": "tput reset && elm make src/client/Main.elm --optimize --output app/app.js",
    "start:server": "cargo run --bin pustaka",
    "seed": "cargo run --bin seed",
    "start": "npm run watch"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "elm": "0.19.0-bugfix2",
    "elm-format": "0.8.0"
  }
}
